import { LoaderWrapper, LoaderContent } from './loader';

const Loader = () => {
  return (
    <LoaderWrapper>
      <LoaderContent></LoaderContent>
    </LoaderWrapper>
  );
};

export default Loader;
